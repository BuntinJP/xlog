{
  "name": "xlog",
  "module": "src/bun_public_server.ts",
  "type": "module",
  "scripts": {
    "start": "hugo serve -p 3000 -b http://localhost:3000",
    "serve": "cd public && php -S localhost:3000",
    "build": "hugo -b http://localhost:3000",
    "build:prod" : "hugo -b https://www.xlog.systems && cp public/sitemap.xml static/sitemap.xml",
    "rhel:serve": "cd public && python3 -m http.server 3000",
    "rhel:build": "hugo -b https://dev.buntin.xyz"
  },
  "dependencies": {
    "autoprefixer": "^10.4.17",
    "postcss-cli": "^11.0.0",
    "postcss-import": "^16.0.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}